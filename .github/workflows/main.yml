name: Gold & Egg Price Tracker

on:
  push:
    branches: ['**']      # 调试期放宽；上线后可收紧到 main
  workflow_dispatch:
  schedule:
    - cron: '0 2 * * *'   # 每天 UTC 02:00（北京时间 10:00）执行一次

jobs:
  update-prices:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # 需要写权限以提交数据文件
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 获取完整历史以便提交

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -V
          pip install requests beautifulsoup4

      - name: Fetch gold and egg prices
        run: |
          python scripts/gold_egg_price.py

      - name: Generate HTML report
        run: |
          python scripts/generate_html.py

      - name: Commit and push data
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/price_history.json index.html
          git diff --staged --quiet || git commit -m "🤖 自动更新价格数据 $(date +'%Y-%m-%d %H:%M')"
          git push

      - name: Send email via Gmail SMTP
        # 只在定时任务或手动触发时发送邮件，推送代码时不发送
        if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
        env:
          SMTP_HOST: smtp.gmail.com
          SMTP_PORT: 587
          GMAIL_USERNAME: ${{ secrets.GMAIL_USERNAME }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
        run: |
          python scripts/send_email.py